<div class="post-new-bg">
  <div class="container py-5">

    <!-- 👇 今あるコードをそのまま -->
    <div class="cover-new">
      <div class="cover-overlay">
        <div class="posts-new">

          <h1 class="page-title">今日の記録</h1>

          <%= form_with model: @post, local: true, html: { multipart: true } do |f| %>

            <div class="mb-3">
              <%= f.text_area :content,
                    rows: 5,
                    class: "form-control",
                    placeholder: "今日感じていることを、ひとことで。\nうまく書こうとしなくて大丈夫です。" %>
            </div>

            <div class="mb-3 border-top pt-3">
              <label class="form-label text-muted small">
                写真を添える（任意）
              </label>
              <%= f.file_field :image,
                    class: "form-control form-control-sm" %>
            </div>

            <p class="small text-muted mb-3">
              ※ この投稿は「◯月◯日の記録」として残ります
            </p>

            <%= f.hidden_field :status, value: "published", id: "post_status" %>

            <div class="text-center">
              <%= f.submit "今日の記録を残す",
                    class: "btn btn-primary px-4 me-2",
                    onclick: "document.getElementById('post_status').value='published'" %>

              <%= f.submit "下書きにする",
                    class: "btn btn-outline-secondary px-4",
                    onclick: "document.getElementById('post_status').value='draft'" %>
            </div>

          <% end %>

        </div>
      </div>
    </div>
    <!-- 👆 ここまで -->

  </div>
</div>
