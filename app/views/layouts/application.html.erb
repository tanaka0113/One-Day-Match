<!DOCTYPE html>
<html>
<head>
  <title>OneDayMatch</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
</head>

<body>
  <header>
    <div>
      <%= link_to root_path, class: "text-white nav-link", id: "head-top" do %>
        OneDayMatch
      <% end %>
    </div>

    <div><%= link_to "トップ", root_path, class: "text-white nav-link" %></div>
    <div><%= link_to "投稿する", new_post_path, class: "text-white nav-link" %></div>
    <div><%= link_to "投稿一覧", posts_path, class: "text-white nav-link" %></div>
  </header>

  <%= yield %>
</body>
</html>
